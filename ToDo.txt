20140502
	UpdateUserHomepage not working
		MODIFIED UserDatabase.updateUserData to first do an insert if necessary
		STILL not working.
	NEXT
		FIX that.
		Make a mock user
			Change that user's ID
			If it works, change it back.